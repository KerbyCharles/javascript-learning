/* distancia*/
let distancia = Number(prompt("INforme a distancia km: "));

/*Velocidade*/
let velocidade = Number(prompt("Informe a velocidade média: "));

/*Calculo o tempo estimado de chegar*/
const temp_estimado_da_viagem = distancia/velocidade

/*Rsultado*/
alert("O tempo estimado da viagem é hrs "+ temp_estimado_da_viagem)