/*Exercicio 22#*/

const lista_nomes = ['Eduardo','Caio', 'Bianca','Camila'];
/*let nome_input = prompt("DIgite o nome: ");

const resultado = lista_nomes.find(item => item == nome_input);
if (resultado){
    alert(`Este aluno  está inscrito ${Resultado}`)
} else {
    alert(`O ${nome_input} não está inscrito`)
}*/



/*Exemplo como adicionar*/
/*lista_nomes.push('André')
lista_nomes.forEach(item => console.log(item));
lista_nomes.map(item => console.log(item+" Aluno Senai"))*/


/*Exemplo para mostra posição da lista*/
const lista_mista = ['ALuno', 1, true,new Array('Eduardo', 5)];
lista_mista.forEach(item)
